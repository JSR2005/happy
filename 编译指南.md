# 📦 毕设项目编译指南与依赖管理

## 项目依赖关系图

```
ROS2 Humble
  ├─ rclcpp (C++)
  ├─ rclpy (Python)
  ├─ std_msgs
  ├─ geometry_msgs
  ├─ sensor_msgs
  ├─ tf2
  └─ tf2_ros

外部依赖
  ├─ OpenCV 4.x
  │  ├─ cv::dnn (推理引擎)
  │  ├─ cv::core (矩阵运算)
  │  └─ cv::imgproc (图像处理)
  ├─ Eigen 3.x (线性代数)
  ├─ PCL (点云处理)
  └─ Boost (工具库)

项目包依赖关系
  ├─ vision_interfaces (消息定义) ✅
  │  ├─ Detection.msg
  │  ├─ DetectionArray.msg
  │  ├─ SkeletonJoint.msg
  │  └─ Skeleton.msg
  │
  ├─ vision_module (感知层)
  │  ├─ 依赖: vision_interfaces, OpenCV, tf2
  │  ├─ yolo_detector_node ✅
  │  ├─ hand_eye_calibration_node ✅
  │  └─ skeleton_detector_node ⏳
  │
  ├─ force_control (力控制层)
  │  ├─ 依赖: Eigen, tf2
  │  └─ admittance_controller_node ✅
  │
  ├─ gripper_module (执行层)
  │  ├─ 依赖: control_msgs, ros2_control
  │  └─ gripper_controller_node ✅
  │
  └─ rm_example (主控制层)
     ├─ 依赖: vision_module, force_control, gripper_module, MoveIt2
     └─ main_controller (主协调节点) ✅

双臂协同层 (可选)
  └─ dual_arm_coordinator
     └─ 依赖: rm_example, MoveIt2
```

---

## 推荐编译顺序

### 步骤 1: 检查环境

```bash
# 1. 验证 ROS2 安装
source /opt/ros/humble/setup.bash
ros2 --version
# 预期输出: ROS 2 Humble Hawksbill

# 2. 检查编译工具
colcon --version
# 预期输出: colcon 0.12.x 或更高

# 3. 验证依赖工具
which rosdep
which ament
```

### 步骤 2: 安装系统依赖

```bash
# 1. 更新包管理器
sudo apt-get update

# 2. 安装 ROS2 依赖
sudo apt-get install -y \
    ros-humble-std-msgs \
    ros-humble-geometry-msgs \
    ros-humble-sensor-msgs \
    ros-humble-tf2 \
    ros-humble-tf2-ros \
    ros-humble-tf2-geometry-msgs \
    ros-humble-cv-bridge \
    ros-humble-image-transport \
    ros-humble-rclcpp-components

# 3. 安装 OpenCV
sudo apt-get install -y libopencv-dev python3-opencv

# 4. 安装 Eigen
sudo apt-get install -y libeigen3-dev

# 5. 安装编译工具
sudo apt-get install -y build-essential cmake git

# 6. 安装 MoveIt2
sudo apt-get install -y ros-humble-moveit

# 7. 安装 Gazebo
sudo apt-get install -y gazebo ros-humble-gazebo-ros
```

### 步骤 3: 检查工作空间结构

```bash
# 进入工作空间
cd /home/jsr/ros2_ws

# 验证结构
tree -L 2 src/
# 预期输出:
# src/
# ├── ros2_rm_robot/
# │   ├── vision_module/
# │   ├── force_control/
# │   ├── gripper_module/
# │   └── rm_example/
# └── vision_interfaces/

# 验证所有 package.xml 存在
find src -name "package.xml" -exec echo "✅ {}" \;
```

### 步骤 4: 按顺序编译

#### 4.1 编译消息定义包 (第一步)

```bash
# 这必须首先编译,因为其他包依赖这些消息

cd /home/jsr/ros2_ws

colcon build \
    --packages-select vision_interfaces \
    --event-handlers console_cohesion+ \
    --cmake-args -DCMAKE_BUILD_TYPE=Release

# 验证成功
echo "source install/setup.bash" >> ~/.bashrc
source install/setup.bash

# 检查消息定义是否可用
ros2 msg list | grep -i detection
ros2 msg list | grep -i skeleton
```

#### 4.2 编译力控制包

```bash
colcon build \
    --packages-select force_control \
    --event-handlers console_cohesion+ \
    --cmake-args -DCMAKE_BUILD_TYPE=Release

# 验证
ros2 node list  # 应该可以看到节点
```

#### 4.3 编译视觉模块

```bash
colcon build \
    --packages-select vision_module \
    --event-handlers console_cohesion+ \
    --cmake-args -DCMAKE_BUILD_TYPE=Release

# 验证
find install/vision_module/lib -name "*_node" -o -name "*_component*"
```

#### 4.4 编译夹爪模块

```bash
colcon build \
    --packages-select gripper_module \
    --event-handlers console_cohesion+ \
    --cmake-args -DCMAKE_BUILD_TYPE=Release

# 验证
ls -la install/gripper_module/lib/gripper_module/
```

#### 4.5 编译主控制模块 (最后)

```bash
colcon build \
    --packages-select rm_example \
    --event-handlers console_cohesion+ \
    --cmake-args -DCMAKE_BUILD_TYPE=Release

# 验证
ls -la install/rm_example/lib/rm_example/
```

#### 4.6 全量编译验证

```bash
# 清理之前的构建
rm -rf build install log

# 一次性全量编译
colcon build \
    --event-handlers console_cohesion+ \
    --cmake-args -DCMAKE_BUILD_TYPE=Release

# 验证所有包都编译成功
echo "=== 编译结果 ==="
if [ -d "install" ]; then
    echo "✅ 编译成功"
    ls install/ | wc -l
    echo "个包已安装"
else
    echo "❌ 编译失败"
fi
```

---

## 编译故障排查

### 问题 1: 找不到依赖包

**错误信息**:
```
CMake Error at /opt/ros/humble/share/catkin/cmake/catkinConfig.cmake:83 (find_package):
  Could not find the following Catkin packages:
```

**解决**:
```bash
# 方案 1: 使用 rosdep 安装所有依赖
sudo rosdep install -i --from-path src --rosdistro humble -y

# 方案 2: 手动安装
sudo apt-get install ros-humble-[package-name]

# 方案 3: 检查 package.xml 中的依赖声明
grep -A5 "build_depend" src/*/package.xml
```

### 问题 2: OpenCV 版本冲突

**错误信息**:
```
OpenCV version mismatch: Expected 4.x.x but found 3.x.x
```

**解决**:
```bash
# 检查已安装的 OpenCV 版本
pkg-config --modversion opencv4

# 如果是 OpenCV 3,升级到 4
sudo apt-get install libopencv-dev
```

### 问题 3: ONNX 运行时找不到

**错误信息**:
```
undefined reference to 'cv::dnn::readNetFromONNX'
```

**解决**:
```bash
# 检查 CMakeLists.txt 中的 OpenCV 链接
grep "opencv_dnn" src/vision_module/CMakeLists.txt

# 或者手动链接
target_link_libraries(yolo_detector_node ${OpenCV_LIBS})

# 验证
pkg-config --cflags --libs opencv4
```

### 问题 4: C++ 标准版本

**错误信息**:
```
error: 'structured_bindings' are only available with '-std=c++17'
```

**解决**:
```bash
# 在 CMakeLists.txt 中设置 C++ 标准
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
```

### 问题 5: 编译超时

**错误信息**:
```
Killed (timeout)
```

**解决**:
```bash
# 分别编译各模块而不是全量编译
colcon build --packages-select vision_module --parallel-workers 2

# 或者增加 CMake 并行度
colcon build --cmake-args -DCMAKE_BUILD_PARALLEL_LEVEL=4
```

---

## 编译性能优化

### 1. 使用预编译二进制

```bash
# 只编译本地包,使用二进制依赖
colcon build \
    --mixin release \
    --event-handlers console_cohesion+
```

### 2. 并行编译

```bash
# 使用多个 CPU 核心
colcon build \
    --parallel-workers 4 \
    --event-handlers console_cohesion+
```

### 3. 增量编译

```bash
# 跳过不变的包
colcon build \
    --event-handlers console_cohesion+ \
    --skip-packages force_control  # 如果这个包没改变
```

### 4. 发布模式编译

```bash
# 生成优化后的代码 (减少调试信息)
colcon build \
    --cmake-args -DCMAKE_BUILD_TYPE=Release \
    --event-handlers console_cohesion+
```

---

## 编译输出验证

### 检查编译成功

```bash
#!/bin/bash

echo "🔍 验证编译结果..."

# 检查是否有错误
if grep -r "error:" build/ 2>/dev/null | head -5; then
    echo "❌ 编译存在错误!"
    exit 1
fi

# 检查是否有关键警告
if grep -r "undefined reference" build/ 2>/dev/null | head -5; then
    echo "⚠️ 编译存在链接错误!"
    exit 1
fi

# 检查可执行文件
echo ""
echo "✅ 检查可执行文件:"
find install -type f -perm /111 -name "*_node" -o -name "*_component*" | while read f; do
    if file "$f" | grep -q "ELF"; then
        echo "  ✅ $(basename $f)"
    fi
done

# 检查库文件
echo ""
echo "✅ 检查库文件:"
find install -name "*.so" -o -name "*.a" | head -10 | while read f; do
    echo "  ✅ $(basename $f)"
done

echo ""
echo "✅ 编译验证完成!"
```

保存为 `verify_build.sh`:
```bash
chmod +x verify_build.sh
./verify_build.sh
```

---

## 开发工作流编译

### 快速开发循环

```bash
#!/bin/bash

# 快速编译和测试单个模块

PACKAGE=${1:-vision_module}

echo "🔨 编译 $PACKAGE..."

cd /home/jsr/ros2_ws

# 1. 编译
colcon build --packages-select "$PACKAGE" --event-handlers console_cohesion+ || exit 1

# 2. 加载环境
source install/setup.bash

# 3. 列出节点
echo "📋 可用节点:"
ros2 pkg prefix "$PACKAGE"

# 4. 快速测试
echo "🧪 快速测试..."
# 这里可以添加单元测试命令

echo "✅ 完成!"
```

### 模块间依赖验证

```bash
# 检查 vision_module 依赖
ros2 pkg list | xargs -I {} bash -c \
    "grep -l vision_module /opt/ros/humble/share/{}/package.xml 2>/dev/null && echo {}" \
    | sort | uniq
```

---

## CI/CD 构建脚本

### 自动化构建脚本

```bash
#!/bin/bash
# 文件: build.sh

set -e  # 错误时退出

echo "📦 毕设项目自动化构建"
echo "时间: $(date)"

cd /home/jsr/ros2_ws

# 1. 清理
echo "🧹 清理旧构建..."
rm -rf build install log

# 2. 环境准备
echo "🔧 准备环境..."
source /opt/ros/humble/setup.bash

# 3. 安装依赖
echo "📥 检查依赖..."
sudo rosdep install -i --from-path src --rosdistro humble -y

# 4. 编译
echo "🔨 开始编译..."
colcon build \
    --event-handlers console_cohesion+ \
    --cmake-args -DCMAKE_BUILD_TYPE=Release \
    --parallel-workers 4

# 5. 验证
echo "✅ 验证编译结果..."
source install/setup.bash

# 6. 运行测试
echo "🧪 运行单元测试..."
colcon test --event-handlers console_cohesion+ || true

# 7. 生成报告
echo "📊 生成编译报告..."
echo "编译完成时间: $(date)" > build_report.txt
echo "编译状态: SUCCESS" >> build_report.txt

echo ""
echo "✅ 编译完成!"
echo "📦 可执行文件位置: $(pwd)/install"
```

使用:
```bash
chmod +x build.sh
./build.sh
```

---

## 编译时间参考

| 操作 | 预计时间 | 优化后 |
|------|---------|--------|
| 首次全量编译 | 5-10 分钟 | 3-5 分钟 |
| 增量编译 (修改 1 文件) | 1-2 分钟 | <30 秒 |
| vision_module 单独编译 | 2-3 分钟 | 1-2 分钟 |
| 清理编译 | 10-15 分钟 | 5-8 分钟 |

---

## 最佳实践

✅ **DO:**
- 定期运行 `colcon build` 验证编译状态
- 使用 Release 模式用于最终测试
- 并行编译以节省时间
- 检查所有依赖都已安装
- 保持 CMakeLists.txt 整洁

❌ **DON'T:**
- 不要手动编辑 build/ 目录中的文件
- 不要在多个终端同时编译
- 不要忽视编译警告
- 不要跳过依赖安装步骤
- 不要使用系统 Python 包管理器安装 ROS 包

---

**编译成功是开发的第一步! 🎯 让我们开始吧!**

